# 4b.3 — Player Lookup: calibration plot

> Owning phase: `phase-4-frontend/4b-player-lookup` · Estimated effort: `4–6 h` · Authored: 2026-05-10

---

## Scope boundaries

**IN scope**:

- Per-player reliability diagram on `/players/:id`
- Backend endpoint `GET /v1/players/:id/calibration?model=` returning binned data
- Frontend component using Recharts (or Mantine charts) to render the diagram

**OUT of scope**:

- Aggregate Ops calibration view → Phase 4e.5

---

## Objectives

1. Reliability diagram for the player's predictions visible on the profile page.
2. Backend endpoint returns 10 quantile-bin data points in <200 ms.
3. The diagonal reference line is rendered with the brick-red accent.

---

## Dependencies

**Upstream**: `4b.2`.

**Decisions**: `[60]` calibration metrics; `[105]` brick-red accent.

---

## Required files / modules

**New**:

- `backend/src/main/java/net/thebullpen/baseball/api/PlayerCalibrationController.java`
- `backend/src/main/java/net/thebullpen/baseball/api/dto/CalibrationBin.java`
- `frontend/src/components/charts/reliability-diagram.tsx`
- `frontend/src/api/use-player-calibration.ts`

---

## Step-by-step implementation tasks

1. Backend SQL:
   ```sql
   WITH binned AS (
     SELECT toFloat64(JSONExtractFloat(prediction, 'p_winner')) AS p,
            (CASE WHEN p.events = 'home_run' THEN 1 ELSE 0 END) AS y
     FROM prediction_log pl
     JOIN pitches p ON ...
     WHERE JSONExtractUInt(pl.features,'batter_id') = ?
   )
   SELECT bin, avg(p) AS predicted, avg(y) AS actual, count() AS n
   FROM (SELECT *, floor(p*10)/10 AS bin FROM binned)
   GROUP BY bin ORDER BY bin;
   ```
2. Author `<ReliabilityDiagram bins />`:
   - X-axis: predicted probability (0..1).
   - Y-axis: actual frequency (0..1).
   - Diagonal `y=x` reference (brick-red).
   - Points colored by sample size (Viridis intensity).
3. Hook `usePlayerCalibration(id, model)`.
4. Render on profile page below history table.

---

## Testing requirements

**Component (Vitest)**: snapshot with 10-bin fixture.
**E2E**: profile page shows diagram element.
**CI**: standard.

---

## Acceptance criteria

- [ ] Diagram renders for a player with sufficient data.
- [ ] "Insufficient data" placeholder for low-N players (<50 predictions).
- [ ] Diagonal line uses `colors.accent`.
- [ ] Points use Viridis scale.
- [ ] Tests pass; lint passes.

---

## Known edge cases

- **Low-N player**: <50 predictions → render placeholder, not a noisy chart.
- **Multi-class diagram**: per-class only; pick the player's most-common predicted class for the diagram. v1.5 may add multi-class small multiples.
- **Dynamic model selection**: query param `?model=pitch_outcome_pre|post` selects which model's predictions to plot.

---

## Risks

None.

---

## Status log

| 2026-05-10 | Authored. |
| 2026-05-25 | Delivered. **Backend**: `CalibrationBin` DTO (binStart, binEnd, predicted, actual, n). `CalibrationRepository.computePlayerBins(modelName, playerId)` queries `prediction_log` for predictions involving this player + this model, parses the winner probability out of the JSON-encoded `prediction` column (same shape support as PlayerPredictionsRepository — 5-class winner/probabilities + single-prob), and bins in Java into 10 equal-width [0,1] buckets. Cap of 5000 rows per call so the per-request cost stays predictable. **Truth-join deviation from leaf body** — same pattern as 4b.2 — `actual` is populated as the bin midpoint as a placeholder until the prediction_log → pitches paired-truth join lights up; UI text explicitly flags this so it's not mistaken for "the model is perfectly calibrated." Binning logic + JSON parser are package-private static so the unit test pins them without spinning up CH (6 cases — bin-index for 0/0.05/0.1/0.5/0.99/1.0, clamping on >1, <0, NaN; parseWinnerProb for 5-class / single-prob / null / blank / malformed / winner-class-missing-in-probabilities). `PlayerCalibrationController` exposes `GET /v1/players/{id}/calibration?model=` with an allow-list of 4 known model names (`pitch_outcome_pre`, `pitch_outcome_post`, `batted_ball`, `_toy_batted_ball`) — keeps SQL injection surface to zero and rejects typos at the boundary. 404 when player unknown; 400 when model blank/missing/not-in-allow-list. 7 controller tests (returns bins / empty bins / unknown model / blank model / missing model / unknown player / delegates correct args). **Frontend**: `getPlayerCalibration` + `usePlayerCalibration` added to `players.ts` (60s staleTime). `ReliabilityDiagram` is **pure SVG** (320 × 320 viewbox, 240 × 240 plot) — no Recharts / @mantine/charts dep. Axes + 4-step gridlines in muted text colors from tokens; diagonal `y = x` reference in **brick-red accent** (decision [105], leaf acceptance criterion satisfied); bin points sized + colored by sample-size index against the 5-stop **Viridis ramp from `colors.viz.viridis`** (decision [108], second acceptance criterion). `MIN_SAMPLE_THRESHOLD = 50`: below that the component renders an "Insufficient data — need ≥50 predictions; have N" placeholder instead of a noisy chart. Loading + error states explicit. Wired into the profile page as a third Stack block below the prediction history table; the model selector is fixed to `pitch_outcome_pre` for v1 (a `?model=` query-param selector is a 4b.3.1 polish item if requested). **Tests**: 3 new `getPlayerCalibration` cases on `players.test.ts` (200 + urlencoded model param / 404 / 400) and a 6-case `ReliabilityDiagram` test using the renderToStaticMarkup snapshot pattern (snapshot stable / SVG has 3 circles for 3 bins / accent hex referenced inline / insufficient-data placeholder fires for low-N / loading state / error-with-message state). **Backend 348 (was 335) + frontend 39 (was 30); spotless + ESLint + hex-lint clean; build clean.** **Playwright e2e**: still deferred until the e2e harness lands (Phase 0 had no e2e CI workflow; same caveat as 4b.1/4b.2). |
