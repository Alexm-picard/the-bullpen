# 4c.4 — Polished heatmap iteration ("fine → memorable")

> Owning phase: `phase-4-frontend/4c-park-explorer` · Estimated effort: `8–14 h` · Authored: 2026-05-10

---

## Scope boundaries

**IN scope**:

- Iterative polish on the 30-park grid:
  - Replace single-tint overlay with a Viridis colormap (decision `[106]`)
  - Show the predicted landing-zone on each stadium SVG (a small dot at `(distance_ft, spray_angle)`)
  - Numeric P(HR) typography: JetBrains Mono, tabular figures
  - Park name in display serif at appropriate size
  - Reorder grid by HR factor descending so the eye lands on best-HR parks first
  - Hover state: highlights one park; shows a small detail panel with full 5-class probabilities
- Visual ambition allowed here (decision `[102]`)

**OUT of scope**:

- Storybook / a11y audit — Phase 5.4 / 5.5

---

## Objectives

1. The grid feels "memorable" rather than "fine" — design pass executed deliberately.
2. Park ordering reflects predicted HR likelihood (most-HR-likely top-left).
3. Hover detail panel works on desktop; touch-tap analog for mobile.
4. Frame budget: <16 ms per slider update (60fps target on grid re-render).

---

## Dependencies

**Upstream**: `4c.1`–`4c.3`.

**Decisions**: `[98]`, `[102]`, `[106]`.

---

## Required files / modules

**New**:

- `frontend/src/components/parks/park-thumbnail-polished.tsx` (replaces 4c.2's basic thumbnail)
- `frontend/src/components/parks/park-detail-panel.tsx`
- `frontend/src/components/parks/_viridis.ts` (colormap helper)

**Modified**:

- `parks-page.tsx` — sorts and renders polished thumbnails; manages hover state.

---

## Step-by-step implementation tasks

1. Build the Viridis colormap helper (lookup table 256 stops).
2. Replace the single-tint `<rect>` overlay with a Viridis-mapped fill based on P(HR).
3. Compute landing zone per park: simple deterministic computation from the canonical input + park-mean conditions (or just take the median of physics simulator output).
4. Render a small dot at the landing point on each SVG.
5. Reorder grid client-side by descending P(HR) on every refetch.
6. Author `<ParkDetailPanel />`: hovering a thumbnail expands a small panel showing 5-class probability bars (P(out), P(1B), P(2B), P(3B), P(HR)), plus the park name in display serif.
7. Optimize re-render: memoize thumbnails so only the changed ones re-render. Use `React.memo` keyed on `(parkId, probHr)`.
8. Profile: `<ProfilerProvider />` measures render duration; assert <16ms per update on a mid-tier laptop.
9. Visual review: take screenshots; iterate until the page reads "editorial-data" not "SaaS chrome".

---

## Testing requirements

**Component**: hover state opens detail panel; snapshot of polished thumbnail.
**Performance**: render-time test with 30 thumbnails ≤ 16 ms (vitest with React profiler).
**E2E**: hover behavior on desktop; tap behavior on mobile.
**CI**: standard.

---

## Acceptance criteria

- [ ] Viridis colormap applied.
- [ ] Park ordering reflects descending P(HR).
- [ ] Detail panel works on hover (desktop) and tap (mobile).
- [ ] 30-tile re-render <16 ms.
- [ ] No hex codes (lint).
- [ ] Visual review: side-by-side with 4c.2 shows clear improvement.

---

## Known edge cases

- **Reordering jitter**: when HR probabilities are close, tiny slider moves can reorder the grid. Add a stable sort with a secondary key (alphabetical park id) to dampen.
- **Mobile viewport**: 6×5 grid → 5 cols on desktop, 3 cols on tablet, 2 cols on phone. Mantine breakpoints handle.
- **Color contrast on light viridis stops**: lightest stops can clash with the warm off-white background. Outline thumbnails with a 1px border (decision `[107]`: borders not shadows).
- **Hover on touch**: simulate via `onTouchStart`. Detail panel auto-dismisses after 3s.

---

## Risks

None directly closed.

---

## Status log

| 2026-05-10 | Authored. |
| 2026-05-25 | Delivered. **Files**: `frontend/src/components/parks/_viridis.ts` is a real interpolated 5-anchor Viridis colormap (sRGB-space piecewise-linear lerp — perceptual non-uniformity is below what's visible at 160-px tiles); `park-thumbnail-polished.tsx` is a React.memo'd component with the Viridis fill (opacity = 0.25 + 0.55·p), a brick-red landing-zone dot, JetBrains-Mono tabular-figures P(HR) row, park name in display-serif heading, 1px border per decision [107], and hover state that emits `onHoverChange(parkId)` upward; `park-detail-panel.tsx` shows the hovered park's name (display serif), altitude, LF/CF/RF fence depths, and a P(HR) bar with the Viridis-mapped fill. `estimate-landing.ts` (split out for the eslint `react-refresh/only-export-components` rule the same way as `format-timestamp.ts` was) is a deterministic landing-distance approximation calibrated to 110 mph / 28° → 400 ft; the real range model is 2c.5 work. `parks-page.tsx` upgraded: client-side stable sort (descending P(HR), tiebreak alphabetical parkId — dampens reordering jitter under tiny slider tweaks per the leaf's "Reordering jitter" edge case), `Grid` split with the thumbnails left and the detail panel right at md+ breakpoints, `useCallback` for the hover handler so memoized thumbnails don't see a new reference. **Two scope deviations from the leaf body**: (1) the 5-class probability bar (P(out)/P(1B)/P(2B)/P(3B)/P(HR)) — the toy batted-ball model only emits P(HR); the 5-class breakdown lives with the pitch-outcome head, not batted balls. Panel renders a single P(HR) bar at large size with a footnote that the 5-class fills in when 2c.5's 30-park MLP lands. The panel's structure stays the same — same Paper, same caption row — so 2c.5 is a content swap, not a redesign. (2) The 16ms render-time profiler assertion was skipped — Vitest + React profiler harness isn't bootstrapped on this repo; `React.memo` keyed on (parkId, probHr, isLoading, landingDistanceFt, sprayAngleDeg) is in place to satisfy the underlying re-render efficiency goal, and the 16ms target becomes verifiable when the e2e/Playwright harness lands in Phase 5. **Two minor implementation fixes during the build**: (a) `<Grid gutter>` is `<Grid gap>` in Mantine 9 (gutter was Mantine 6 / 7); (b) Viridis-anchor test asserts case-insensitively because the colormap output is lowercase hex while `tokens.colors.viz.viridis[*]` is uppercase. **Tests**: 5-case `_viridis.test.ts` (boundary clamping / NaN / 7-char hex for any in-range / each anchor) + 7-case `park-thumbnail-polished.test.tsx` (rendering all three primitives / landing dot toggles with probHr / loading hides tint+dot / landing-distance estimator: calibrated to ~400 ft at 110/28, lower-speed → shorter, off-sweet-spot → shorter, floor at 60 ft). Frontend 65 (was 53); all checks clean. |
