# 3b.5 — Async batched prediction logger (full version)

> Owning phase: `phase-3-ml-wrapper/3b-ab-routing` · Estimated effort: `4–6 h` · Authored: 2026-05-10

---

## Scope boundaries

**IN scope**:

- Replace 1.7's primitive logger with the full version
- Schema additions: `request_id` UUID, `role` enum, `feature_hash`, structured `features` (LowCardinality + columnar where possible), proper `model_version_id`
- Migration `V015__prediction_log_v2.sql` ALTERs the schema additively
- Backfill of `model_version_id=0` rows is OK (recent only)
- Logger writes per-role rows in shadow mode

**OUT of scope**:

- Drift computation reading from this → Phase 3c
- Reconciliation against registry → 3a.5

---

## Objectives

1. `prediction_log` carries everything needed by 3c drift jobs and 3b.4 experiment evaluation.
2. Schema is forward-compatible with future model architectures.
3. Async logger handles 2× volume in shadow mode without dropping under normal load.
4. Backwards-compatible: existing 1.7 rows still queryable.

---

## Dependencies

**Upstream**: `1.7`, `3a.2`, `3b.3`.

**Decisions**: `[30]` async batched logging, drop-on-overflow.

---

## Required files / modules

**New**:

- `backend/src/main/resources/db/migration/clickhouse/V015__prediction_log_v2.sql`
- `backend/src/main/java/net/thebullpen/baseball/inference/PredictionLogEventV2.java` (record, replaces V1)

**Modified**:

- `AsyncPredictionLogger` — accepts V2 record, writes V2 schema.
- `PredictionLogWriter` — V2 INSERT.
- All call sites in `InferenceRouter` (3b.3) — produce V2 records.

---

## Step-by-step implementation tasks

1. V015:
   ```sql
   ALTER TABLE prediction_log
     MODIFY COLUMN role Enum8('champion'=1,'challenger'=2,'shadow'=3),
     ADD COLUMN IF NOT EXISTS feature_hash String AFTER model_version_id,
     COMMENT COLUMN features 'JSON-encoded; columnar variant in 3b.6 candidate v1.5';
   -- TTL set in 1.7 already; no change.
   ```
2. Author `PredictionLogEventV2` record with all V2 fields. Mark V1 record `@Deprecated`.
3. Update `AsyncPredictionLogger`:
   - Accept V2 events.
   - Increase queue size to 20K (was 10K) to absorb shadow-mode 2× volume.
4. Update `PredictionLogWriter` to insert V2 columns.
5. Update `InferenceRouter` (from 3b.3) to construct V2 events with correct `role` and `model_version_id`.
6. Backfill / migration consideration: old V1 rows (where role wasn't set) get `'champion'` via DEFAULT — verify in V015.
7. Tests:
   - `AsyncPredictionLoggerV2Test`: enqueue 100 V2 events; all flushed.
   - `PredictionLogWriterV2IT`: V2 INSERT against Testcontainers.
   - Backwards-compat: query a V1 row → returns with `role='champion'`.

---

## Testing requirements

**Unit**: V2 row construction.
**Integration**: V2 INSERT works; shadow-mode 2× load doesn't overflow.
**CI**: `test-java.yml`.

---

## Acceptance criteria

- [ ] V015 applied; schema reflects V2.
- [ ] V2 logger replaces V1 in production code.
- [ ] Shadow-mode load test: 1K req/s sustained; drop counter = 0.
- [ ] V1 rows still queryable.
- [ ] Tests pass.

---

## Known edge cases

- **Schema rollback**: if you need to roll back V015, you'd lose the new columns' data. Forward-only rule applies — don't roll back.
- **`features` as JSON String column**: still stringy in v1; columnar restructure deferred to v1.5.
- **Backfill of `feature_hash` for V1 rows**: leave NULL. Drift computations skip NULLs.
- **Volume**: shadow mode + post-pitch model means 4× request volume → 4× rows. Verify TTL still leaves manageable disk.

---

## Risks

None directly closed.

---

## Status log

| 2026-05-10 | Authored. |
| 2026-05-24 | Most of this leaf was already done in 1.7 + 3b.3: V004's `prediction_log` already has `role Enum8('champion','challenger','shadow')` + `feature_hash` + `request_id` UUID + `model_version` LowCardinality(String); 1.7's `AsyncPredictionLogger` already has the bounded queue + drop-on-overflow + flusher + metrics + shutdown drain; 3b.3's batted-ball router rewire already produces CHAMPION + SHADOW pairs in shadow mode (verified by `InferenceRouterIT`). Real 3b.5 scope was the remaining delta: (a) `model_version_id` Nullable(Int64) column added via V012 ClickHouse migration (additive + nullable for backwards compat — V1 rows from 1.7 + legacy-fallback dispatches set null; ModelLoader-resolved versions carry the FK); (b) `PredictionLogEvent` record gained `Long modelVersionId` field with a backward-compat constructor that defaults to null (existing call sites + tests untouched); (c) `PredictionLogWriter` INSERT column list + bind expanded with `setNull(BIGINT)` for the nullable case; (d) `PredictBattedBallController` shadow + champion enqueue paths populate the FK from `RoutedPrediction.servingVersionId() / shadowVersionId()` (legacy `-1L` → null); (e) `AsyncPredictionLogger` default queue capacity bumped 10K → 20K via `bullpen.inference.log.queue-capacity` property (absorbs shadow-mode 2× volume per leaf objective 3). **Deferred from this commit**: the V015 schema name in the leaf body is wrong (existing migrations went V010+); used V012 as the next CH slot. The `features` columnar restructure stays JSON (leaf "Known edge cases" v1.5 deferral). Pitch controller stays on the legacy direct-bean path (consistent with 3b.3's deferral). Tests: 3 new `AsyncPredictionLoggerTest` cases — V1 legacy constructor carries null modelVersionId, V2 constructor carries explicit FK, shadow-mode 2× volume (1000 reqs × 2 rows = 2K events) with periodic flushing leaves drop count at 0 against the bumped 20K capacity AND verifies role distribution is balanced 1:1 CHAMPION/SHADOW. **208 backend tests total — 203 pass + 5 skipped (Docker-gated SnapshotStorageIT); spotless clean.** Phase 3b A/B routing is now complete end-to-end (3b.1 routing config + cache → 3b.2 Murmur3 bucketer → 3b.3 router + ModelLoader + batted-ball rewire → 3b.4 experiment lifecycle + metric math → 3b.5 V012 schema + 2× capacity). 3c.1 (drift detection) is the next leaf — note 3b.4's `PairedPredictionFetcher` interface is the shared seam 3c will implement against ClickHouse. |
