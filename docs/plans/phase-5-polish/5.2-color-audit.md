# 5.2 — Color audit ★ Discipline gate

> Owning phase: `phase-5-polish` · Estimated effort: `2–4 h` · Authored: 2026-05-10

---

## Scope boundaries

**IN scope**:

- `pnpm lint:hex-codes` MUST return zero hits in `src/` outside `src/design/`
- Replace any inline hex codes with token references
- Verify accent (`#B53D2C`) used sparingly (not as a default text color)
- Verify Viridis used wherever data viz appears
- Confirm no `box-shadow` in any component (decision `[107]`); use 1 px borders
- Confirm no `linear-gradient` (decision `[107]`)

**OUT of scope**:

- Adding new color tokens — only do if a real design need emerges (rare)
- A/B testing color palette — locked

**Hard rule** (CLAUDE.md rule 1, decision `[105]`–`[107]`): hex codes in components are defects.

---

## Objectives

1. `pnpm lint:hex-codes` returns 0 hits.
2. Zero `box-shadow` rules in `src/`.
3. Zero `linear-gradient(` in `src/`.
4. Documented audit in PR description.

---

## Dependencies

**Upstream**: `4a` (tokens), all 5 page leaves.

**Decisions**: `[105]`–`[107]`; CLAUDE.md rule 1.

---

## Required files / modules

**Modified**:

- Any component file containing inline hex codes, shadows, or gradients.
- `tokens.ts` — only if a new token is genuinely required (rare).

---

## Step-by-step implementation tasks

1. Run `pnpm lint:hex-codes` and treat each hit as a defect:
   - Replace `#fff` → `colors.bgElevated`.
   - Replace bespoke greens → `colors.viz.viridis[N]`.
   - Replace red → `colors.accent` (only if semantic — alarms, disagreements).
2. Run `grep -rn "box-shadow\|drop-shadow" frontend/src/`. Replace each with a 1px border (`border border-bg-emphasis`).
3. Run `grep -rn "linear-gradient" frontend/src/`. Replace each with a flat color (or remove).
4. Verify accent usage is restrained: `grep -rn "accent" frontend/src/components | wc -l` should be a small number (handful of intentional uses, e.g., disagreement marker, primary CTA).
5. Visual review of data-viz components: confirm Viridis used (not rainbow / not custom).
6. Take screenshots of any visually affected components; document in PR.

---

## Testing requirements

**Lint**: `pnpm lint:hex-codes` returns 0 hits; CI gates on this.
**Visual**: manual screenshot review.

---

## Acceptance criteria

- [ ] `pnpm lint:hex-codes` 0 hits.
- [ ] `grep box-shadow|drop-shadow|linear-gradient` returns no production code matches.
- [ ] Accent used in <10 places.
- [ ] Viridis used wherever data viz colors appear.
- [ ] PR documents the audit + changes.

---

## Known edge cases

- **Mantine internal styles** may use hex / shadows — not in our `src/`, won't be flagged. OK.
- **External SVGs** (stadiums) — `stroke="currentColor"` so they inherit theme. No hex inside.
- **3rd-party charting library defaults** — verify Recharts colors are tokens, not defaults.
- **Migration cost**: if many violations exist, allocate the full 4 hours; don't skim.

---

## Risks

CLAUDE.md rule 1 — closes operationally for v1.

---

## Status log

| 2026-05-10 | Authored. |
| 2026-05-25 | Delivered. **Audit results**: (1) `npm run lint:hex-codes` returns 0 hits outside `src/design/`. (2) `grep box-shadow|drop-shadow|linear-gradient` in `frontend/src` returns 0 hits. The only previous offenders lived in `src/App.css` — a leftover Vite-template file (180+ lines of `.counter` / `.hero` / `.social-link` styles, `box-shadow` rule, social-link gradients) that wasn't imported from anywhere in `src/`. **Deleted** the entire file as dead code; no behavior change. (3) Accent usage in production code: 8 references (`grep colors.accent | grep -v .test. | grep -v /design/`) — well under the leaf's <10 target. The 8 sites are: token-sample (visual ground-truth), reliability-diagram (diagonal y=x line), pitch-card (✗ marker text + 2px disagreement rule), retrain-queue (truncated error message text), drift sparkline (line stroke), park-thumbnail-polished (landing-zone dot + hover outline). All semantic — alarm, disagreement, or "this is the project's chromatic anchor" usage; none decorative. (4) Viridis ramp used everywhere data-viz appears: park-thumbnail-polished (sample-size shading on bin points), reliability-diagram (Viridis intensity), park-thumbnail (single-tint v1 uses viz.categorical[4]), drift sparkline (`colors.accent` for the line itself but background is theme-neutral). (5) Stadium SVGs use `stroke="currentColor"` so they inherit theme color — confirmed across all 30 files. No hex inside any stadium SVG. **Phase 5.4** will pair this with the contrast-ratio + a11y check; this leaf's discipline gate (CLAUDE.md rule 1) is operationally closed. Frontend 93 tests pass; backend unchanged. |
