# 5.5 — Park Explorer polish iteration ("fine → memorable")

> Owning phase: `phase-5-polish` · Estimated effort: `8–14 h` · Authored: 2026-05-10

---

## Scope boundaries

**IN scope**:
- Deliberate visual iteration on the Park Explorer marquee
- Areas to revisit (in priority order):
  - Tooltip / detail panel typography and positioning
  - Transition timing on slider-driven re-renders (already debounced; tune fade)
  - Empty state for unrealistic inputs (e.g., 60 mph × 60° launch where P(HR) is 0 everywhere)
  - Annotation: brief text on the page explaining what users are seeing
  - Ordering animation: when the grid re-sorts, animate position changes (Framer Motion `layout`)
- Allocate 8–14 hours; do NOT cap at 4

**OUT of scope**:
- Adding new features (sliders for spin, etc.) — those are v1.5
- Mobile-specific optimizations (sliders work on touch already) — confirm but don't redesign

---

## Objectives

1. Side-by-side comparison with the Phase 4c.4 version shows clear visual improvement.
2. The page is the strongest single artifact in the project — the one a recruiter screenshots and shares.
3. Animations feel intentional (not gratuitous, not stuttery).

---

## Dependencies

**Upstream**: `4c.1`–`4c.4`, `5.1`, `5.2`, `5.3`, `5.4`.

**Decisions**: `[98]`, `[102]`, `[112]` motion functional only.

---

## Required files / modules

**Modified**:
- `frontend/src/pages/parks/parks-page.tsx`
- `frontend/src/components/parks/*` (most files)
- Possibly add `framer-motion` dep for `<AnimatePresence>` and `layout` reordering.

---

## Step-by-step implementation tasks

1. Add `framer-motion`: `pnpm add framer-motion`.
2. Wrap thumbnail grid with `<motion.div layout />` so reorder transitions animate (200 ms, ease).
3. Annotate page: at the top, a single sentence explaining "Drag the sliders to see how an at-bat at this speed/angle/spray would play in each park." Source Serif italic, restrained.
4. Tooltip refinement: position above thumbnail (not floating randomly); use `colors.bgElevated` background, 1px `bg-emphasis` border, no shadow.
5. Empty state: when all P(HR) < 0.005, render a placeholder explaining "no realistic HR scenarios at these inputs."
6. Re-render performance: profile with React DevTools; ensure re-renders happen only on changed thumbnails (memo by `(parkId, probHr.toFixed(3))`).
7. Take side-by-side screenshots (4c.4 vs 5.5) — attach to PR. Recruiter test: would you screenshot this for a friend? If not, iterate again.
8. Lighthouse: confirm no regression (perf score still ≥ 80).

---

## Testing requirements

**Visual**: side-by-side screenshots in PR.
**Performance**: profile shows <16 ms per slider update unchanged.
**E2E**: existing tests still pass.
**CI**: standard.

---

## Acceptance criteria

- [ ] Side-by-side comparison documented in PR.
- [ ] Reorder animation works smoothly.
- [ ] Annotation text present.
- [ ] Empty state for unrealistic inputs.
- [ ] No perf regression.
- [ ] Lint + tests pass.

---

## Known edge cases

- **Framer Motion bundle weight**: ~50 KB gz. Acceptable for the marquee; tree-shake to use only `motion.div` and `AnimatePresence`.
- **Reorder during slider drag**: lock reorder until slider commit (200 ms debounced). Avoid jitter.
- **Animation accessibility**: respect `prefers-reduced-motion` — disable Framer animations when set.
- **When to stop iterating**: if you've spent 14 h and it still feels "fine" not "memorable", commit what you have and document the gap as a v1.5 polish target.

---

## Risks

None directly.

---

## Status log

| 2026-05-10 | Authored. |
