# 5.7 — Drift postmortem template ★ Centerpiece artifact

> Owning phase: `phase-5-polish` · Estimated effort: `3–4 h to author template; 4–8 h per real postmortem` · Authored: 2026-05-10

---

## Scope boundaries

**IN scope**:

- A reusable template for drift incident postmortems at `ops/runbooks/drift-postmortem-template.md`
- Process: when a drift event fires, copy template → `docs/postmortems/<date>-<slug>.md` → fill in
- First real postmortem authored when the first drift event lands (Phase 5+ during operation)
- Postmortem linked from README and Ops dashboard

**OUT of scope**:

- The drift detector itself → Phase 3c
- Process for promoting findings into design changes — happens organically via `decisions.md` ADRs

**This is the centerpiece resume artifact** (decision `[82]`). Quality > quantity.

---

## Objectives

1. Template exists and is unambiguous to fill in.
2. First real postmortem produced when the first drift event lands.
3. Postmortem includes: dashboards, root-cause analysis, decisions made, code changes, lessons.

---

## Dependencies

**Upstream**: `3c.7` (alerting), all of Phase 3c.

**Decisions**: `[82]`.

---

## Required files / modules

**New**:

- `ops/runbooks/drift-postmortem-template.md`
- `docs/postmortems/.keep`
- `docs/postmortems/2026-XX-XX-<slug>.md` (when first event lands)
- README link to the postmortems directory

---

## Step-by-step implementation tasks

1. Author the template:

   ```markdown
   # Postmortem: <one-line title>

   - **Date range**: YYYY-MM-DD to YYYY-MM-DD
   - **Severity**: page / notice
   - **Affected model**: model_name v<N>
   - **Detected by**: detector job name + threshold
   - **Operator**: <name>
   - **Summary** (3 sentences max)

   ## Timeline (UTC)

   | When             | What                 |
   | ---------------- | -------------------- |
   | YYYY-MM-DDTHH:MM | Drift detector fired |
   | YYYY-MM-DDTHH:MM | Investigation began  |
   | ...              | ...                  |

   ## What happened

   <prose>

   ## Dashboards

   ![Calibration drift](path/to/grafana-snapshot-1.png)
   ![PSI heatmap](path/to/grafana-snapshot-2.png)

   ## Drift metrics that fired

   | Metric            | Value | Threshold                      |
   | ----------------- | ----- | ------------------------------ |
   | calibration_error | 0.052 | 0.030 (1.5× of training 0.020) |

   ## Hypotheses + tests

   1. Hypothesis A: <text>
      - Test: <how we tested>
      - Result: confirmed / rejected
   2. Hypothesis B: ...

   ## Root cause (5-Whys)

   1. Why did calibration drift? Because ...
   2. Why? Because ...
   3. ...
   4. Root cause: <answer>

   ## Resolution

   <what we did>

   ## What changed in the system

   - [ ] Code change: <link to PR>
   - [ ] Test added: <link>
   - [ ] Alert tightened: <details>
   - [ ] Decision logged: `[N]` in decisions.md

   ## Lessons

   <prose>
   ```

2. Document the process:
   - When an alert fires, the operator copies the template, fills it in alongside the investigation, and commits.
   - Postmortems live under `docs/postmortems/` with date-prefixed filenames.
3. Link from README ("Operating evidence" section) and from Ops dashboard ("Postmortems" tab — extension to 4e.5 if time allows; or just in README).
4. **First real postmortem**: when the first drift event lands during operation, follow the template. Honest reporting — don't sanitize.

---

## Testing requirements

**Manual**: dry-run the template by simulating a synthetic drift event (using 3c.6's CLI) and walking through the template.

---

## Acceptance criteria

- [ ] Template exists at `ops/runbooks/drift-postmortem-template.md`.
- [ ] `docs/postmortems/` directory exists with a `.keep` file.
- [ ] Process documented in template.
- [ ] First real postmortem authored within 30 days of first detected event.
- [ ] README links to `docs/postmortems/`.

---

## Known edge cases

- **Synthetic drift events in production**: when running drills, label the postmortem as "Drill — not real production drift" and store under `docs/postmortems/drills/`.
- **No drift events ever**: unlikely (an MLB season has obvious mid-season shifts: ABS, weather, lineup changes). If by mid-season no drift fired, lower thresholds and re-run synthetic tests to verify the detector isn't broken.
- **Postmortem timing**: don't write too soon. Wait until the resolution is in place. But don't wait too long — memory fades within days.

---

## Risks

None directly closed; this leaf consummates decision `[82]`.

---

## Status log

| 2026-05-10 | Authored. |
| 2026-05-25 | Delivered (template + scaffolding). **`ops/runbooks/drift-postmortem-template.md`** is the reusable template with: header (date range, severity, model, detector, operator, 3-sentence summary), UTC timeline table, dashboards section (with subdirectory convention `docs/postmortems/<slug>/` for PNGs), "drift metrics that fired" table, hypotheses+tests numbered list, 5-Whys root-cause block, resolution prose, **checklist** for "what changed in the system" (code change PR, regression test, alert tightened, decision logged, ADR if architectural, runbook update), lessons prose, references. Drill-vs-real convention documented at the top. **`docs/postmortems/.keep`** + **`docs/postmortems/README.md`** anchor the directory and document the process: copy template, fill alongside the investigation (don't wait), commit on resolution (don't sanitize), promote findings to `decisions.md` / ADRs / runbooks where worth carrying forward. Drill postmortems explicitly welcomed (prefixed `drill-`) — they're the only thing keeping the drift detector honest before the first real event. README's "Operating evidence" section (5.6) already links to `docs/postmortems/`. **Deferred** (gated on real operation, not on this leaf): (1) The first real postmortem authored within 30 days of the first detected event — needs the season to start + a drift trigger to fire. (2) Synthetic-drift dry-run of the template using a Phase-3c.6 CLI — that CLI isn't built yet; pairs with the actual 3c.7 alerting work, not this template leaf. Both deferrals consummate decision [82] over the season, not in this commit. Template + directory + process are in place; the first event lands them. |
