"""Offline evaluation surfaces for the pitch heads (pre / post).

Home of the rule-13 holdout accuracy backfill (``backfill_accuracy``) and the
shared eval helpers (``_shared``) extracted from the parity fixtures so the
offline evals and the fixtures can converge on one implementation.
"""
