"""Python-side helpers for interacting with the Java registry service.

The registry itself lives in the JVM; this package holds the
shared-contract pieces that must stay byte-for-byte aligned with the
Java implementation in ``net.thebullpen.baseball.registry``.
"""
